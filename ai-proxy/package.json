{
  "name": "rcd-ai-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "AI proxy for the relationship clarity dashboard. Verifies Supabase JWTs and forwards to OpenAI.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.80.0"
  },
  "dependencies": {
    "jose": "^5.9.0"
  }
}
